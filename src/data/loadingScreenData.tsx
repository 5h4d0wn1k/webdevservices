import React from 'react';

interface LoadingScreenDataType {
  headingText: string;
  loadingText: string;
  loadingTip: string;
}

export const loadingScreenData: LoadingScreenDataType = {
  headingText: "Shadownik(Swnk)",
  loadingText: "Loading Experience",
  loadingTip: "Crafting digital experiences that push boundaries...",
}; 