import React from 'react';

interface PageTransitionDataType {
  loadingDuration: number;
  loadingText: string;
}

export const pageTransitionData: PageTransitionDataType = {
  loadingDuration: 2000,
  loadingText: "Shadownik(Swnk)",
}; 